import { useState, useEffect } from "react";

// function Data() {
//   const [data, setData] = useState("");

//   useEffect(() => {
//     fetch("http://metaphorpsum.com/paragraphs/1/4")
//       .then((res) => res.json())
//       .then((data) => setData(data));
//   }, []);

//   return data;
// }
// export default Data;

const string =
  "Recent controversy aside, the montane romanian reveals itself as a dicky output to those who look. Though we assume the latter, a wider snowman's kendo comes with it the thought that the chaffy eight is a pharmacist. The schizo weapon reveals itself as a controlled bee to those who look. As far as we can estimate, authors often misinterpret the sudan as a ghastful gallon, when in actuality it feels more like a roundish mother-in-law. To be more specific, one cannot separate appendixes from doggone greies. A bandana is the bean of a knowledge.";

export default string;

//http://metaphorpsum.com/
